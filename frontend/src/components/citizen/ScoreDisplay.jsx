 import React from 'react'
import ScoreGauge from '../../ScoreGauge' // Import direct parent for now as it's in src/components

// Wrapper component if needed, or just re-export
export default ScoreGauge
